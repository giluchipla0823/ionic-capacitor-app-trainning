import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-capacitor-app-trainning',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
